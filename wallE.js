+function wallE(window) {
    var we = {};

    we.collect = function(data, type) {
      // push data to sets
      // if fullfill sets,cancle the event listeners
    };

    we.init = function() {
      //register events
    };

    // utils
    // zip
    function pakmsg(obj) {
      var str = obj.toString();
      // a zip algorithm
      return str;
    }

    // ajax
    function send(msg, cb) {
      //call ajax send
      //cb(ajax.resp)
    }

    function clickHandler() {

    }

    function moveHandler() {
    }

    function cookieParser() {
    }

  }(window)
